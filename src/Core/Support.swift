import Foundation

// Hand-written support for the oxidtr-generated scaffold in this directory.
// The Swift target emits `defaultStr()` calls for String fields but not the
// factory itself. Not regenerated — keep it here.
// ponytail: empty string default; give it a realistic sample if fixtures need one.
func defaultStr() -> String { "" }
