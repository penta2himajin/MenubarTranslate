# macOS
.DS_Store
# Env / secrets (see Common Prohibitions in AGENTS.md)
.env
.env.*
# Local model weights — do not commit multi-GB GGUF blobs
*.gguf
models/*
# ...but Alloy specs under models/ are source of truth — track them
!models/*.als
# Build output
build/
dist/
target/
node_modules/
# SwiftPM
.build/
.swiftpm/
# Vendored xcframeworks — built locally by scripts/build-llama-xcframework.sh
vendor/
# Agent-harness scratch (devloop/GLM)
.devloop/
.claude-glm-task.md
